// SPDX-License-Identifier: MIT
#pragma once

#include <Arduino.h>

const char WEB_UI[] PROGMEM = R"HTML(
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <meta name="theme-color" content="#080b18">
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <title>Leaf Lights</title>
  <script>try{document.documentElement.dataset.theme=localStorage.getItem('leaflights-theme')||'midnight'}catch(e){document.documentElement.dataset.theme='midnight'}</script>
  <style>
    :root{color-scheme:dark;--bg:#080b18;--panel:#10162a;--panel2:#171f38;--line:#313b5f;--text:#f7f8ff;--muted:#a5aecb;--accent:#6f8cff;--accent2:#7957e8;--accent-contrast:#fff;--radial:#1a285b;--button:#151c34;--button-hover:#1d294a;--nav:#0d1223;--field:#0c1122;--deep:#050815;--note:#101936;--glow:#5c78ff55;--red:#ef6262;--amber:#ffc568;--shadow:0 18px 50px #0008;font-family:Inter,ui-rounded,"SF Pro Rounded",system-ui,sans-serif}
    html[data-theme=firework]{--bg:#0e0912;--panel:#1b101d;--panel2:#29152c;--line:#5a3152;--text:#fff7fb;--muted:#c2a8ba;--accent:#ff536b;--accent2:#496dff;--accent-contrast:#fff;--radial:#50162a;--button:#281527;--button-hover:#3a1b34;--nav:#170d1a;--field:#160b18;--deep:#08050d;--note:#2b1224;--glow:#ff456755}
    html[data-theme=solar]{--bg:#100d08;--panel:#21180e;--panel2:#302112;--line:#62472c;--text:#fff9ed;--muted:#c8b69b;--accent:#ffb52e;--accent2:#ff663d;--accent-contrast:#281500;--radial:#5a350d;--button:#2b1e11;--button-hover:#3b2a16;--nav:#191108;--field:#1a1209;--deep:#090703;--note:#31200d;--glow:#ff9d3255}
    *{box-sizing:border-box}html{background:var(--bg);scroll-behavior:smooth;font-size:15px}body{margin:0;overflow-x:hidden;background:radial-gradient(circle at 50% -10%,var(--radial) 0,transparent 36rem),var(--bg);color:var(--text);min-height:100vh;line-height:1.4;transition:background .25s,color .25s;-webkit-tap-highlight-color:transparent}button,input,select{font:inherit}button{color:inherit}button:focus-visible,input:focus-visible,select:focus-visible{outline:2px solid var(--accent);outline-offset:2px}.app{width:100%;max-width:1180px;margin:auto;padding:0 20px 96px}.card,.hero>*,.editor-layout>*{min-width:0}
    .topbar{position:sticky;top:0;z-index:20;display:flex;align-items:center;gap:16px;padding:15px 0;background:linear-gradient(color-mix(in srgb,var(--bg),transparent 4%) 70%,transparent);backdrop-filter:blur(12px)}.brand{display:flex;align-items:center;gap:10px;min-width:max-content}.brandmark{display:grid;place-items:center;width:40px;height:40px;border-radius:13px;background:linear-gradient(145deg,var(--accent),var(--accent2));box-shadow:0 8px 26px var(--glow)}.brandmark svg{width:25px}.brand strong{font-size:1.05rem}.brand small{display:block;color:var(--muted);font-size:.7rem;margin-top:1px}.spacer{flex:1}.connection{display:flex;align-items:center;gap:8px;color:var(--muted);font-size:.8rem}.dot{width:8px;height:8px;border-radius:50%;background:var(--amber);box-shadow:0 0 12px currentColor}.dot.online{background:var(--accent)}
    .btn{border:1px solid var(--line);background:var(--button);padding:11px 16px;border-radius:12px;font-weight:750;cursor:pointer;min-height:46px;transition:background .18s,border-color .18s,transform .18s}.btn:hover{border-color:var(--accent);background:var(--button-hover)}.btn:active{transform:translateY(1px)}.btn.primary{background:linear-gradient(135deg,var(--accent2),var(--accent));border-color:var(--accent);color:var(--accent-contrast);box-shadow:0 8px 24px var(--glow)}.btn.ghost{background:transparent}.btn.danger{color:#ffd9d9;border-color:#713e3e;background:#2b1717}.btn.small{padding:8px 12px;min-height:40px;font-size:.86rem}.iconbtn{width:46px;height:46px;padding:0;border-radius:50%}.desktop-nav{display:flex;gap:4px;padding:4px;border:1px solid var(--line);background:var(--nav);border-radius:14px}.desktop-nav button{border:0;background:transparent;padding:9px 13px;border-radius:10px;color:var(--muted);font-weight:700;cursor:pointer}.desktop-nav button.active{background:var(--panel2);color:var(--text)}
    main{min-height:65vh}.view{display:none}.view.active{display:block}.eyebrow{text-transform:uppercase;letter-spacing:.16em;font-size:.68rem;font-weight:850;color:var(--accent)}h1{font-size:clamp(2rem,5.5vw,4rem);line-height:1.02;margin:7px 0 10px;letter-spacing:-.045em}h2{font-size:1.25rem;margin:0}h3{margin:0;font-size:1rem}.lead{color:var(--muted);max-width:620px;line-height:1.5}.section-head{display:flex;align-items:end;justify-content:space-between;gap:16px;margin:28px 0 12px}.section-head p{margin:4px 0 0;color:var(--muted);font-size:.87rem}
    .hero{display:grid;grid-template-columns:1.1fr .9fr;gap:15px;margin-top:14px}.card{background:linear-gradient(150deg,var(--panel2),var(--panel));border:1px solid var(--line);border-radius:19px;padding:19px;box-shadow:0 1px 0 #ffffff0a inset,0 14px 38px #0002}.hero-copy{padding:28px}.hero-copy .btn{margin-top:15px}.control-card{overflow:hidden}.live-label{display:flex;justify-content:space-between;align-items:center;margin-bottom:12px}.now{color:var(--accent);font-size:.78rem;font-weight:800}.preview{display:flex;align-items:center;gap:6px;overflow:hidden;min-height:54px;padding:11px;border-radius:14px;background:var(--deep);border:1px solid var(--line)}.preview.large{min-height:76px;gap:8px}.led{flex:0 0 auto;width:18px;height:18px;border-radius:50%;background:var(--panel);border:1px solid var(--muted);box-shadow:0 0 0 transparent;transition:background .12s,box-shadow .12s,opacity .12s}.preview.large .led{width:22px;height:22px}.led.on{border-color:#fff;box-shadow:0 0 12px var(--c),0 0 25px color-mix(in srgb,var(--c),transparent 55%)}
    .range-row{display:grid;grid-template-columns:auto 1fr 44px;align-items:center;gap:12px;margin-top:18px}.range-row output{font-variant-numeric:tabular-nums;text-align:right;color:var(--muted)}input[type=range]{width:100%;height:44px;accent-color:var(--accent)}.swatches{display:flex;gap:10px;overflow:auto;padding:12px 2px 3px;scrollbar-width:none}.swatch{width:44px;height:44px;flex:0 0 auto;border-radius:13px;border:2px solid #ffffff5c;background:var(--c);cursor:pointer;box-shadow:0 5px 14px #0008}.custom-color{position:relative;background:conic-gradient(red,#ff0,#0f0,#0ff,#00f,#f0f,red)}.custom-color input{position:absolute;inset:0;opacity:0;width:100%;height:100%;cursor:pointer}
    .home-schedule{width:100%;display:flex;align-items:center;gap:12px;text-align:left;margin-top:14px;padding:13px 14px;border:1px solid var(--line);border-radius:14px;background:var(--field);cursor:pointer}.home-schedule:hover{border-color:var(--accent);background:var(--button-hover)}.home-schedule>i{display:grid;place-items:center;width:34px;height:34px;flex:0 0 auto;border-radius:11px;background:color-mix(in srgb,var(--accent),transparent 78%);color:var(--accent);font-style:normal;font-size:1.1rem}.home-schedule>span{min-width:0;flex:1}.home-schedule small,.home-schedule strong{display:block}.home-schedule small{color:var(--muted);font-size:.74rem;margin-top:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.home-schedule>b{color:var(--accent);font-size:.8rem}
    .zone-picker{display:flex;gap:9px;overflow:auto;padding:3px 1px 4px;scrollbar-width:none}.chip{min-height:44px;border:1px solid var(--line);background:var(--nav);color:var(--muted);padding:9px 13px;border-radius:999px;white-space:nowrap;cursor:pointer;font-weight:700}.chip.active{color:var(--accent-contrast);background:var(--accent);border-color:var(--accent)}
    .pattern-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:13px}.pattern-card{padding:16px}.pattern-title{display:flex;align-items:start;justify-content:space-between;gap:9px;margin-bottom:11px}.pattern-title span{color:var(--muted);font-size:.72rem;text-transform:uppercase;letter-spacing:.09em;font-weight:800}.pattern-meta{display:flex;gap:7px;flex-wrap:wrap;margin:10px 0}.tag{border:1px solid var(--line);background:var(--nav);color:var(--muted);padding:4px 7px;border-radius:8px;font-size:.72rem}.actions{display:flex;gap:7px}.actions .btn{flex:1}.empty{text-align:center;padding:44px 18px;color:var(--muted)}
    .editor-layout{display:grid;grid-template-columns:minmax(0,1.25fr) minmax(280px,.75fr);gap:18px}.field{display:block;color:var(--muted);font-size:.8rem;font-weight:700}.field input,.field select{width:100%;border:1px solid var(--line);background:var(--field);color:var(--text);border-radius:11px;padding:11px;margin-top:6px;min-height:44px}.form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:13px}.movement-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin-top:14px}.movement{border:1px solid var(--line);background:var(--nav);border-radius:14px;padding:10px 7px;cursor:pointer;color:var(--muted);font-size:.78rem;font-weight:750}.movement.active{border-color:var(--accent);background:color-mix(in srgb,var(--accent),var(--panel) 78%);color:var(--text);box-shadow:0 0 0 1px var(--accent) inset}.movement .mini{display:flex;height:26px;align-items:center;justify-content:center;gap:3px;margin-bottom:7px;overflow:hidden}.movement .mini i{width:7px;height:7px;border-radius:50%;background:var(--line)}.palette-editor{display:flex;gap:9px;overflow:auto;padding:9px 2px 12px}.color-edit{position:relative;flex:0 0 auto;width:46px;height:46px;border-radius:14px;border:2px solid #ffffff7a;background:var(--c);overflow:hidden}.color-edit input{position:absolute;inset:0;width:100%;height:100%;opacity:0;cursor:pointer}.color-edit button{position:absolute;right:-2px;top:-2px;width:19px;height:19px;border:0;border-radius:50%;background:#000b;color:white;cursor:pointer;font-size:12px;line-height:18px;padding:0}.add-color{display:grid;place-items:center;background:var(--panel2);color:var(--accent);font-size:1.4rem;border-style:dashed;cursor:pointer}.editor-actions{display:flex;gap:10px;margin-top:18px;flex-wrap:wrap}.editor-actions .btn{flex:1;min-width:130px}.sticky-preview{position:sticky;top:96px}.summary{margin:18px 0;padding:14px;border-radius:14px;background:var(--nav);border:1px solid var(--line);color:var(--muted);line-height:1.55}.summary strong{color:var(--text)}
    details.card{padding:0;overflow:hidden;margin-bottom:13px}details summary{list-style:none;cursor:pointer;padding:19px 22px;font-weight:800;display:flex;justify-content:space-between}details summary::-webkit-details-marker{display:none}details summary:after{content:'+';color:var(--accent);font-size:1.25rem}details[open] summary:after{content:'−'}details .details-body{border-top:1px solid var(--line);padding:20px 22px}.note{border-left:3px solid var(--accent);background:var(--note);padding:13px 14px;border-radius:0 10px 10px 0;color:var(--muted);font-size:.86rem;line-height:1.5;margin:12px 0}.note.warning{border-color:var(--amber);background:#261d0b}.zone-config{display:grid;grid-template-columns:74px 1fr 120px 100px;gap:10px;align-items:end;padding:12px 0;border-bottom:1px solid var(--line)}.check{display:flex;align-items:center;gap:8px;min-height:44px}.check input{width:20px;height:20px;accent-color:var(--accent)}.statusbox{background:var(--deep);border:1px solid var(--line);border-radius:12px;padding:14px;white-space:pre-wrap;color:var(--muted);font:12px/1.55 ui-monospace,monospace;overflow:auto}.settings-actions{margin-top:15px}.wled-state{font-size:.8rem;color:var(--muted);margin-top:10px}.upload-progress{width:100%;height:12px;margin-top:14px;accent-color:var(--accent)}.firmware-state{margin-top:10px;color:var(--muted);font-size:.84rem}.release-list{display:grid;gap:11px;margin-top:12px}.release-card{border:1px solid var(--line);background:var(--field);border-radius:14px;padding:15px}.release-card header{display:flex;justify-content:space-between;align-items:start;gap:10px}.release-card .notes{white-space:pre-wrap;color:var(--muted);font-size:.84rem;line-height:1.5;max-height:180px;overflow:auto;margin:11px 0}.release-card .btn{width:100%}
    .theme-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:11px}.theme-choice{border:1px solid var(--line);background:var(--field);border-radius:15px;padding:14px;text-align:left;cursor:pointer}.theme-choice.active{border-color:var(--accent);box-shadow:0 0 0 1px var(--accent) inset}.theme-choice strong{display:block;margin:10px 0 3px}.theme-choice span{color:var(--muted);font-size:.78rem}.theme-dots{display:flex;gap:6px}.theme-dots i{width:18px;height:18px;border-radius:50%;background:var(--dot);box-shadow:0 0 14px color-mix(in srgb,var(--dot),transparent 45%)}
    .schedule-summary{display:grid;grid-template-columns:1.3fr 1fr 1fr;gap:12px;margin:18px 0}.schedule-stat{padding:17px;border:1px solid var(--line);border-radius:16px;background:var(--field)}.schedule-stat span{display:block;color:var(--muted);font-size:.75rem;text-transform:uppercase;letter-spacing:.08em;font-weight:800}.schedule-stat strong{display:block;margin-top:8px;line-height:1.35}.schedule-list{display:grid;gap:12px}.schedule-card{display:grid;grid-template-columns:1fr auto;gap:12px;align-items:center}.schedule-card p{color:var(--muted);margin:6px 0 0;font-size:.86rem;line-height:1.45}.schedule-card .actions{min-width:230px}.rule-editor{display:none;margin-top:14px}.rule-editor.show{display:block}.day-picker,.zone-checks{display:flex;gap:8px;flex-wrap:wrap;margin:12px 0}.toggle-chip{position:relative}.toggle-chip input{position:absolute;opacity:0;pointer-events:none}.toggle-chip span{display:flex;align-items:center;min-height:44px;border:1px solid var(--line);background:var(--field);color:var(--muted);border-radius:999px;padding:9px 12px;font-size:.8rem;font-weight:800;cursor:pointer}.toggle-chip input:checked+span{background:var(--accent);border-color:var(--accent);color:var(--accent-contrast)}.time-expression{display:grid;grid-template-columns:1fr 1fr;gap:10px}.manual-actions{display:flex;gap:9px;flex-wrap:wrap}.schedule-help{color:var(--muted);font-size:.84rem;line-height:1.5}
    .gate{position:fixed;z-index:120;inset:0;display:none;place-items:center;padding:20px;background:radial-gradient(circle at 50% 0,var(--radial),var(--bg) 55%)}.gate.show{display:grid}body:has(.gate.show){overflow:hidden}.gate-card{width:min(460px,100%);padding:30px}.gate-card h1{font-size:2.25rem}.wizard-card{position:relative;width:min(880px,100%);max-height:calc(100vh - 32px);overflow:auto;padding:26px}.wizard-close{position:absolute;right:18px;top:17px;width:40px;height:40px;border:0;border-radius:50%;background:var(--field);color:var(--muted);font-size:1.35rem;cursor:pointer}.wizard-steps{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:18px 0 25px}.wizard-step{height:5px;border-radius:9px;background:var(--line)}.wizard-step.active{background:var(--accent)}.wizard-panel{display:none}.wizard-panel.active{display:block}.wizard-actions{display:flex;justify-content:space-between;gap:10px;margin-top:24px}.wizard-actions .btn{min-width:120px}.wizard-zone{padding:14px 0;border-bottom:1px solid var(--line)}.wizard-zone-head{display:flex;align-items:center;justify-content:space-between;gap:12px}.wizard-zone-head strong{font-size:.95rem}.wizard-zone-fields{display:grid;grid-template-columns:1fr 120px 100px;gap:10px;align-items:end;margin-top:12px}.wizard-zone:not(.enabled) .wizard-zone-fields{display:none}.security-choice{margin-top:14px;padding:16px;border:1px solid var(--line);border-radius:14px;background:var(--field)}
    .bottom-nav{display:none;position:fixed;z-index:30;bottom:0;left:0;right:0;padding:8px max(10px,env(safe-area-inset-left)) calc(8px + env(safe-area-inset-bottom));background:color-mix(in srgb,var(--bg),transparent 3%);border-top:1px solid color-mix(in srgb,var(--line),transparent 20%);backdrop-filter:blur(22px)}.bottom-nav button{flex:1;min-height:52px;border:0;background:transparent;color:var(--muted);padding:7px 2px;border-radius:12px;font-size:.68rem;font-weight:750}.bottom-nav b{display:block;font-size:1.1rem;margin-bottom:2px}.bottom-nav button.active{color:var(--accent);background:var(--panel2)}.toast{position:fixed;z-index:99;left:50%;bottom:28px;transform:translate(-50%,20px);opacity:0;pointer-events:none;background:var(--text);color:var(--bg);border-radius:12px;padding:12px 17px;font-weight:750;box-shadow:var(--shadow);transition:.22s}.toast.show{opacity:1;transform:translate(-50%,0)}.toast.error{background:#ffdede;color:#4d1111}.update-drawer{position:fixed;z-index:80;left:50%;bottom:18px;width:min(720px,calc(100% - 28px));transform:translate(-50%,calc(100% + 45px));opacity:0;pointer-events:none;background:color-mix(in srgb,var(--panel2),#000 8%);border:1px solid var(--accent);border-radius:18px;padding:17px 18px;box-shadow:var(--shadow);transition:.28s}.update-drawer.show{transform:translate(-50%,0);opacity:1;pointer-events:auto}.update-drawer.error{border-color:var(--red)}.update-drawer.success{border-color:#54d894}.drawer-head{display:flex;align-items:start;gap:14px}.drawer-copy{flex:1}.drawer-copy strong{display:block}.drawer-copy span{display:block;color:var(--muted);font-size:.84rem;margin-top:4px;line-height:1.4}.drawer-close{border:0;background:transparent;color:var(--muted);font-size:1.25rem;cursor:pointer}.drawer-progress{width:100%;height:9px;margin-top:13px;accent-color:var(--accent)}.loading{opacity:.55;pointer-events:none}
    @media(max-width:800px){.app{padding:0 12px 100px}.desktop-nav{display:none}.connection span{display:none}.bottom-nav{display:flex}.toast{bottom:88px;max-width:calc(100% - 28px);text-align:center}.update-drawer{bottom:82px}.topbar{gap:9px;padding:9px 0}.brandmark{width:38px;height:38px;border-radius:12px}.brand small{display:none}.topbar>.btn span{display:none}.topbar>.btn{width:44px;padding:0}.hero,.editor-layout{grid-template-columns:1fr}.hero{margin-top:6px}.control-card{order:-1}.hero-copy{padding:18px}.hero-copy h1{font-size:clamp(2rem,10.5vw,2.8rem)}.hero-copy .btn{width:100%}.card{padding:16px;border-radius:17px}.sticky-preview{position:static}.movement-grid{grid-template-columns:none;grid-template-rows:repeat(2,70px);grid-auto-columns:minmax(128px,40vw);grid-auto-flow:column;overflow-x:auto;scroll-snap-type:x mandatory;padding:2px 2px 7px;scrollbar-width:none}.movement{scroll-snap-align:start}.pattern-grid{grid-template-columns:1fr}.zone-config{grid-template-columns:66px 1fr 1fr;padding:12px;border:1px solid var(--line);border-radius:13px;background:var(--field);margin-bottom:9px}.zone-config .order{grid-column:2/4}.preview.large .led{width:20px;height:20px}.form-grid{grid-template-columns:1fr 1fr}.schedule-summary{grid-template-columns:1fr 1fr;margin:11px 0}.schedule-summary .schedule-stat:first-child{grid-column:1/3}.schedule-stat{padding:12px}.schedule-card{grid-template-columns:1fr}.schedule-card .actions{min-width:0}.manual-actions .btn{flex:1;min-width:170px}details summary{padding:15px 16px}details .details-body{padding:16px}.section-head{margin:22px 0 11px}.view>.section-head:first-child{margin-top:13px}.wizard-card{padding:19px;max-height:calc(100vh - 16px)}.wizard-zone-fields{grid-template-columns:1fr 105px 86px}}
    @media(max-width:430px){h1{font-size:1.95rem}.brand strong{font-size:.96rem}.connection{display:none}.form-grid,.theme-grid{grid-template-columns:1fr}.actions .btn{padding:10px 7px}.preview{gap:5px}.preview.large{gap:6px}.led{width:17px;height:17px}.preview.large .led{width:19px;height:19px}.wizard-zone-fields{grid-template-columns:1fr 1fr}.wizard-zone-fields .field:first-child{grid-column:1/3}.schedule-stat strong{font-size:.88rem}.home-schedule{padding:10px}.home-schedule>b{display:none}.section-head{align-items:center}.section-head>.btn{flex:0 0 auto}.gate{padding:8px}.gate-card{padding:20px}.gate-card .btn.primary{width:100%}.wizard-actions .btn{min-width:0;flex:1}.bottom-nav button{font-size:.63rem}}
    @media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}.led{box-shadow:none!important}}
  </style>
</head>
<body>
<div id="authGate" class="gate"><form class="card gate-card" onsubmit="login(event)"><div class="eyebrow">Leaf Lights</div><h1>Welcome back</h1><p class="lead">Enter your controller password to continue.</p><label class="field">Password<input id="loginPassword" type="password" autocomplete="current-password" required autofocus></label><button class="btn primary settings-actions" type="submit">Sign in</button><div class="firmware-state" id="loginStatus"></div></form></div>
<div id="setupWizard" class="gate"><form id="wizardForm" class="card wizard-card" onsubmit="finishSetup(event)"><div class="eyebrow">First-time setup</div><div class="wizard-steps"><i class="wizard-step"></i><i class="wizard-step"></i><i class="wizard-step"></i><i class="wizard-step"></i></div><section class="wizard-panel"><h1>Welcome to Leaf Lights</h1><p class="lead">This four-step setup configures your lighting zones, network access, optional Oelo/LeafFilter compatibility, and web interface security.</p><div class="note">Fresh controllers broadcast <b>LeafLights-Setup</b> using password <b>LeafLights-Setup</b>. That temporary network turns off when setup finishes.</div></section><section class="wizard-panel"><h1>Set up your zones</h1><p class="lead">Enter logical Oelo fixture counts. Each fixture is transmitted as two UCS1903 pixels.</p><div id="wizardZones"></div></section><section class="wizard-panel"><h1>Connect your controller</h1><p class="lead">Home Wi-Fi provides normal local access. Oelo compatibility is optional and disabled by default.</p><div class="form-grid"><label class="field">Home Wi-Fi name<input name="ssid" id="wizardSsid" autocomplete="username"></label><label class="field">Home Wi-Fi password<input name="wifiPassword" type="password" autocomplete="current-password" placeholder="Leave blank to keep existing"></label></div><div class="security-choice"><label class="check"><input name="compatibilityEnabled" id="wizardCompatibility" type="checkbox" value="1" onchange="renderWizardCompatibility()"> Enable Oelo/LeafFilter compatibility</label><div id="wizardCompatibilityFields" style="display:none"><div class="note warning"><strong>Compatibility tradeoff:</strong> the legacy lighting API does not authenticate individual commands. WPA2 keeps outsiders off the network, so use a unique Wi-Fi password.</div><label class="field">Oelo network password<input name="compatibilityPassword" id="wizardCompatibilityPassword" type="password" minlength="8" maxlength="63" autocomplete="new-password" placeholder="8–63 characters"></label></div></div></section><section class="wizard-panel"><h1>Protect the web interface</h1><p class="lead">Optional. Network passwords already control who can connect; this adds a login screen for the browser interface and management APIs.</p><label class="check security-choice"><input name="protectWebUi" id="wizardProtectWeb" type="checkbox" value="1" onchange="renderWizardWebPassword()"> Require a web interface password</label><div id="wizardWebPasswordFields" class="form-grid" style="display:none;margin-top:14px"><label class="field">Web password<input name="webPassword" id="wizardWebPassword" type="password" minlength="8" maxlength="64" autocomplete="new-password"></label><label class="field">Confirm password<input id="wizardWebPasswordConfirm" type="password" minlength="8" maxlength="64" autocomplete="new-password"></label></div><div class="note">After setup, firmware updates, restarts, and settings use this same browser session. There are no separate update-password prompts.</div></section><div class="wizard-actions"><button id="wizardBackButton" class="btn ghost" type="button" onclick="wizardPrevious()">Back</button><button id="wizardNextButton" class="btn primary" type="button" onclick="wizardNext()">Continue</button><button id="wizardFinishButton" class="btn primary" type="submit">Finish setup</button></div><div class="firmware-state" id="wizardStatus"></div></form></div>
<div class="app">
  <header class="topbar">
    <div class="brand"><div class="brandmark"><svg viewBox="0 0 28 28" fill="none" stroke="white" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M2.5 16.5 8.7 10.3l5.2 5 4.3-4.2 7.3 6.4"/><g fill="white" stroke="none"><circle cx="5.5" cy="20.5" r="1.35"/><circle cx="10" cy="20.5" r="1.35"/><circle cx="14.5" cy="20.5" r="1.35"/><circle cx="19" cy="20.5" r="1.35"/><circle cx="23.5" cy="20.5" r="1.35"/></g></svg></div><div><strong>Leaf Lights</strong><small>Open lighting controller</small></div></div>
    <nav class="desktop-nav" aria-label="Primary"><button data-nav="home">Home</button><button data-nav="patterns">Patterns</button><button data-nav="create">Create</button><button data-nav="schedules">Schedules</button><button data-nav="settings">Settings</button></nav>
    <div class="spacer"></div><div class="connection"><i id="connDot" class="dot"></i><span id="connText">Connecting</span></div><button class="btn danger" onclick="allOff()" aria-label="Turn all zones off">⏻ <span>All off</span></button>
  </header>

  <main>
    <section id="view-home" class="view">
      <div class="hero">
        <div class="card hero-copy"><div class="eyebrow">Outdoor lighting</div><h1>Make tonight<br>look good.</h1><p class="lead">Pick a zone, color, or saved look. Nothing changes until you tap Play.</p><button class="btn primary" onclick="showView('patterns')">Browse patterns</button></div>
        <div class="card control-card"><div class="live-label"><div><div class="eyebrow">Now showing</div><h3 id="nowName">Lights off</h3></div><span class="now" id="nowMode">OFF</span></div><div id="livePreview" class="preview large" data-preview="active"></div><button class="home-schedule" onclick="showView('schedules')"><i>◷</i><span><strong id="homeScheduleStatus">No schedule configured</strong><small id="homeScheduleNext">Open schedules to automate the lights</small></span><b>View ›</b></button><div class="range-row"><span>☀</span><input id="homeBrightness" type="range" min="1" max="204" value="32" oninput="brightnessPreview(this.value)" onchange="setBrightness(this.value)"><output id="brightnessValue">13%</output></div><div class="wled-state" id="powerSafetyLabel">80% safety ceiling · 36 V / 5.6 A supply</div><div class="swatches" id="quickColors"></div></div>
      </div>
      <div class="section-head"><div><h2>Choose zones</h2><p>Select one or more rooflines before playing a color or pattern.</p></div></div><div class="zone-picker" id="homeZones"></div>
      <div class="section-head"><div><h2>Ready to play</h2><p>Quick access to your first saved patterns.</p></div><button class="btn ghost small" onclick="showView('patterns')">See all</button></div><div id="quickPatterns" class="pattern-grid"></div>
    </section>

    <section id="view-patterns" class="view">
      <div class="section-head"><div><div class="eyebrow">Your collection</div><h1>Patterns</h1><p>Preview each look before sending it to your lights.</p></div><button class="btn primary" onclick="newPattern()">＋ New pattern</button></div>
      <div class="zone-picker" id="patternZones"></div><div id="patternGrid" class="pattern-grid" style="margin-top:18px"></div>
    </section>

    <section id="view-create" class="view">
      <div class="section-head"><div><div class="eyebrow">Pattern studio</div><h1 id="editorHeading">New pattern</h1><p>Build a look, preview it, then try it on your selected zones.</p></div></div>
      <div class="editor-layout">
        <div>
          <div class="card"><label class="field">Pattern name<input id="patternName" maxlength="48" placeholder="Summer evening" oninput="readEditor()"></label><div class="section-head"><div><h3>Movement</h3><p id="movementDescription">A fixed repeating palette.</p></div></div><div id="movementGrid" class="movement-grid"></div></div>
          <div class="card" style="margin-top:18px"><div class="section-head" style="margin-top:0"><div><h3>Colors</h3><p>Tap a color to edit it. Use × to remove one.</p></div><span id="colorCount" class="tag">1 color</span></div><div id="paletteEditor" class="palette-editor"></div><div class="swatches" id="editorPresets"></div></div>
          <div class="card" style="margin-top:18px"><div class="form-grid"><label class="field">Speed <output id="speedOut">10</output><input id="patternSpeed" type="range" min="1" max="20" value="10" oninput="speedOut.textContent=this.value;readEditor()"></label><label class="field">Direction<select id="patternDirection" onchange="readEditor()"><option value="F">Forward</option><option value="R">Reverse</option></select></label><label class="field">Gap<input id="patternGap" type="number" min="0" max="255" value="0" oninput="readEditor()"></label><label class="field">Width / other<input id="patternOther" type="number" min="0" max="255" value="0" oninput="readEditor()"></label><label class="field">Pause<input id="patternPause" type="number" min="0" max="255" value="0" oninput="readEditor()"></label></div></div>
        </div>
        <aside class="card sticky-preview"><div class="eyebrow">Live preview</div><h2 id="previewName" style="margin:7px 0 16px">Untitled pattern</h2><div id="editorPreview" class="preview large" data-preview="editor"></div><div id="editorSummary" class="summary"></div><div class="zone-picker" id="editorZones"></div><div class="editor-actions"><button class="btn" onclick="testEditor()">Test on lights</button><button class="btn primary" onclick="saveEditor()">Save pattern</button><button class="btn ghost" onclick="showView('patterns')">Cancel</button></div></aside>
      </div>
    </section>

    <section id="view-schedules" class="view">
      <div class="section-head"><div><div class="eyebrow">Automatic lighting</div><h1>Schedules</h1><p>Weekly routines, holiday overrides, and sunrise-aware timing.</p></div></div>
      <div class="schedule-summary"><div class="schedule-stat"><span>Controller status</span><strong id="scheduleStatus">Loading…</strong></div><div class="schedule-stat"><span>Local time</span><strong id="scheduleNow">—</strong></div><div class="schedule-stat"><span>Next change</span><strong id="scheduleNext">—</strong></div></div>
      <div class="card"><div class="section-head" style="margin-top:0"><div><h2>Manual control</h2><p>Temporary overrides take priority over every schedule.</p></div></div><div class="manual-actions"><button class="btn" onclick="setManualOverride('hold')">Hold current until next event</button><button class="btn" onclick="setManualOverride('off')">Lights off until next event</button><button class="btn ghost" onclick="setManualOverride('clear')">Resume schedule</button></div><div id="manualOverrideStatus" class="firmware-state"></div></div>

      <details class="card" open style="margin-top:16px"><summary>Location and time</summary><div class="details-body"><p class="schedule-help">Location is used only on this controller to calculate sunrise and sunset. The controller gets clock time from the network.</p><form id="scheduleLocationForm" onsubmit="saveScheduleLocation(event)"><div class="form-grid"><label class="field">Time zone<select id="scheduleTimezone"><option value="EST5EDT,M3.2.0/2,M11.1.0/2">Eastern</option><option value="CST6CDT,M3.2.0/2,M11.1.0/2">Central</option><option value="MST7MDT,M3.2.0/2,M11.1.0/2">Mountain</option><option value="MST7">Arizona</option><option value="PST8PDT,M3.2.0/2,M11.1.0/2">Pacific</option><option value="AKST9AKDT,M3.2.0/2,M11.1.0/2">Alaska</option><option value="HST10">Hawaii</option></select></label><span></span><label class="field">Latitude<input id="scheduleLatitude" type="number" min="-90" max="90" step="0.0001" required></label><label class="field">Longitude<input id="scheduleLongitude" type="number" min="-180" max="180" step="0.0001" required></label></div><div class="manual-actions settings-actions"><button class="btn primary" type="submit">Save location</button><button class="btn" type="button" onclick="useBrowserLocation()">Use this device’s location</button></div></form></div></details>

      <div class="section-head"><div><h2>Weekly routine</h2><p>The normal seven-day schedule. Higher-priority holiday rules replace it.</p></div><button class="btn primary" onclick="editWeeklyRule(-1)">＋ Add weekly rule</button></div><div id="weeklyScheduleList" class="schedule-list"></div>
      <form id="weeklyRuleEditor" class="card rule-editor" onsubmit="saveWeeklyRule(event)"><div class="section-head" style="margin-top:0"><div><h2 id="weeklyEditorTitle">Weekly rule</h2><p>Choose days, an on/off window, pattern, and zones.</p></div></div><div class="form-grid"><label class="field">Name<input id="weeklyName" maxlength="48" required placeholder="Weeknight lights"></label><label class="check"><input id="weeklyEnabled" type="checkbox" checked> Enabled</label></div><div id="weeklyDays" class="day-picker"></div><div class="form-grid"><div><h3>Turn on</h3><div id="weeklyOnExpression" class="time-expression"></div></div><div><h3>Turn off</h3><div id="weeklyOffExpression" class="time-expression"></div></div><label class="field">Pattern<select id="weeklyPattern"></select></label><div><span class="field">Zones</span><div id="weeklyZones" class="zone-checks"></div></div></div><div class="editor-actions"><button class="btn primary" type="submit">Save rule</button><button class="btn ghost" type="button" onclick="closeRuleEditor('weekly')">Cancel</button></div></form>

      <div class="section-head"><div><h2>Holiday overrides</h2><p>Annual date windows that replace the weekly routine.</p></div><button class="btn primary" onclick="editHolidayRule(-1)">＋ Add holiday</button></div><div id="holidayScheduleList" class="schedule-list"></div>
      <form id="holidayRuleEditor" class="card rule-editor" onsubmit="saveHolidayRule(event)"><div class="section-head" style="margin-top:0"><div><h2 id="holidayEditorTitle">Holiday override</h2><p>The pattern runs each day in the selected before/after window.</p></div></div><div class="form-grid"><label class="field">Name<input id="holidayName" maxlength="48" required placeholder="Fourth of July"></label><label class="check"><input id="holidayEnabled" type="checkbox" checked> Enabled</label><label class="field">Month<select id="holidayMonth"></select></label><label class="field">Day<input id="holidayDay" type="number" min="1" max="31" required></label><label class="field">Start days before<input id="holidayBefore" type="number" min="0" max="366" value="0"></label><label class="field">End days after<input id="holidayAfter" type="number" min="0" max="366" value="0"></label></div><div class="form-grid"><div><h3>Turn on</h3><div id="holidayOnExpression" class="time-expression"></div></div><div><h3>Turn off</h3><div id="holidayOffExpression" class="time-expression"></div></div><label class="field">Pattern<select id="holidayPattern"></select></label><div><span class="field">Zones</span><div id="holidayZones" class="zone-checks"></div></div></div><div class="editor-actions"><button class="btn primary" type="submit">Save override</button><button class="btn ghost" type="button" onclick="closeRuleEditor('holiday')">Cancel</button></div></form>
      <div class="note" style="margin-top:18px"><strong>Priority:</strong> manual override → holiday override → weekly routine → off. If a holiday window applies, the weekly routine stays suppressed for that whole date range.</div>
    </section>

    <section id="view-settings" class="view">
      <div class="section-head"><div><div class="eyebrow">Controller</div><h1>Settings</h1><p>Daily controls stay simple; installation options live here.</p></div></div>
      <details class="card" open><summary>Appearance</summary><div class="details-body"><div class="theme-grid"><button class="theme-choice" data-theme-choice="midnight" onclick="applyTheme('midnight')"><span class="theme-dots"><i style="--dot:#6f8cff"></i><i style="--dot:#7957e8"></i><i style="--dot:#10162a"></i></span><strong>Midnight</strong><span>Electric blue and violet</span></button><button class="theme-choice" data-theme-choice="firework" onclick="applyTheme('firework')"><span class="theme-dots"><i style="--dot:#ff536b"></i><i style="--dot:#496dff"></i><i style="--dot:#1b101d"></i></span><strong>Firework</strong><span>Hot coral and royal blue</span></button><button class="theme-choice" data-theme-choice="solar" onclick="applyTheme('solar')"><span class="theme-dots"><i style="--dot:#ffb52e"></i><i style="--dot:#ff663d"></i><i style="--dot:#21180e"></i></span><strong>Solar</strong><span>Warm amber and ember</span></button></div><div class="note">Theme selection is saved in this browser, so each phone or computer can have its own look.</div></div></details>
      <details class="card"><summary>WLED realtime sync</summary><div class="details-body"><div class="note"><strong>Exact-frame sync:</strong> the controller broadcasts its rendered pixels using DDP instead of guessing at WLED effect names. Use <b>255.255.255.255</b> to reach every WLED receiver on the LAN, or enter one device IP. A specific IP gives the smoothest playback; broadcast is convenient for multiple receivers but is more vulnerable to Wi-Fi jitter.</div><form id="wledForm"><div class="form-grid"><label class="check"><input name="enabled" id="wledEnabled" type="checkbox" value="1"> Sync to WLED</label><label class="field">Destination<input name="destination" id="wledDestination" value="255.255.255.255" inputmode="decimal"></label><label class="field">Virtual pixel count<input name="pixelCount" id="wledPixels" type="number" min="1" max="1000" value="300"></label><label class="field">Source zone<select name="sourceZone" id="wledSource"><option value="-1">Auto (first active)</option></select></label></div><button class="btn primary settings-actions" onclick="saveWled(event)">Save WLED sync</button></form><div class="wled-state" id="wledStatus"></div><div class="note">On each receiving WLED controller, enable <b>Force max brightness</b> under Sync Interfaces → Realtime and <b>Disable WiFi sleep</b> under WiFi Setup. Set the virtual count to the receiver strip length for a full-span animation.</div></div></details>
      <details class="card"><summary>Firmware updates</summary><div class="details-body"><div class="note warning"><strong>Install only releases from WVandergrift/oelo-lights.</strong> Keep the controller powered until verification completes.</div><div class="note">Firmware changes use the current web session. No separate update password is required.</div><div class="firmware-state" id="firmwareVersion">Current firmware: loading…</div><div class="section-head" style="margin:22px 0 8px"><div><h3>GitHub releases</h3><p>Read the release notes, then choose a version to install.</p></div><button class="btn small" onclick="loadReleases(true)">Check now</button></div><div id="releaseList" class="release-list"><div class="empty">Open this panel while connected to home Wi‑Fi to check releases.</div></div><label class="check" style="margin-top:14px"><input id="automaticUpdates" type="checkbox"> Automatically install stable updates</label><button class="btn settings-actions" onclick="saveAutomaticUpdates(event)">Save automatic update setting</button><div class="firmware-state" id="automaticUpdateStatus"></div><h3 style="margin-top:24px">Upload a local build</h3><label class="field" style="margin-top:12px">Firmware image (.bin)<input id="firmwareFile" type="file" accept=".bin,application/octet-stream"></label><button class="btn primary settings-actions" onclick="uploadFirmware(event)">Upload, verify, and reboot</button><div class="firmware-state" id="firmwareUpdateStatus">No update in progress.</div></div></details>
      <details class="card"><summary>Zones and LED hardware</summary><div class="details-body"><div class="note">Counts are logical Oelo fixtures. Each fixture is transmitted as two UCS1903 pixels. Saving these settings reboots the controller.</div><form id="zonesForm"></form><button class="btn primary settings-actions" onclick="saveZones(event)">Save zones and reboot</button></div></details>
      <details class="card"><summary>Network</summary><div class="details-body"><div class="note" id="compatibilityStatus"></div><form id="networkForm" class="form-grid"><label class="field">Home Wi‑Fi name<input name="ssid" id="ssid" autocomplete="username"></label><label class="field">Home Wi‑Fi password<input name="password" type="password" autocomplete="current-password" placeholder="Leave blank to keep current password"></label><label class="check"><input name="compatibilityApEnabled" id="compatibilityApEnabled" type="checkbox" value="1"> Enable WPA2 Oelo/LeafFilter compatibility network</label><label class="field">Compatibility Wi‑Fi password<input name="compatibilityApPassword" id="compatibilityApPassword" type="password" minlength="8" maxlength="63" autocomplete="new-password" placeholder="Leave blank to keep current password"></label></form><button class="btn primary settings-actions" onclick="saveNetwork(event)">Save network settings and reboot</button></div></details>
      <details class="card"><summary>Web UI access</summary><div class="details-body"><div class="note" id="webAccessStatus"></div><form id="webPasswordForm" class="form-grid"><label class="field">New web password<input id="webPassword" type="password" minlength="8" maxlength="64" autocomplete="new-password"></label><label class="field">Confirm password<input id="webPasswordConfirm" type="password" minlength="8" maxlength="64" autocomplete="new-password"></label></form><button class="btn primary settings-actions" onclick="saveWebPassword(event)">Enable or change password</button><button class="btn ghost settings-actions" id="removeWebPasswordButton" onclick="removeWebPassword(event)">Remove password</button></div></details>
      <details class="card"><summary>Device</summary><div class="details-body"><div class="note">Restarting briefly turns the lights off and reconnects to the configured network.</div><button class="btn settings-actions" onclick="startSetupWizard()">Run setup wizard</button><button class="btn danger settings-actions" onclick="restartDevice(event)">Restart controller</button></div></details>
      <details class="card"><summary>Diagnostics</summary><div class="details-body"><div id="statusBox" class="statusbox">Loading…</div><button class="btn settings-actions" onclick="loadAll()">Refresh status</button></div></details>
    </section>
  </main>
</div>

<nav class="bottom-nav" aria-label="Primary"><button data-nav="home"><b>⌂</b>Home</button><button data-nav="patterns"><b>✦</b>Patterns</button><button data-nav="create"><b>＋</b>Create</button><button data-nav="schedules"><b>◷</b>Schedule</button><button data-nav="settings"><b>⚙</b>Settings</button></nav>
<div id="toast" class="toast" role="status"></div>
<aside id="updateDrawer" class="update-drawer" role="status" aria-live="polite"><div class="drawer-head"><div class="drawer-copy"><strong id="updateDrawerTitle">Firmware update</strong><span id="updateDrawerDetail">Preparing…</span></div><button class="drawer-close" onclick="closeUpdateDrawer()" aria-label="Dismiss update status">×</button></div><progress id="updateDrawerProgress" class="drawer-progress" max="100"></progress></aside>

<script>
const movementInfo={stationary:'A fixed repeating palette.',arcade:'A bright runner with a trailing pixel.',blend:'A moving gradient across the roofline.',bolt:'A short, fast streak with a fading tail.',chase:'Groups of color separated by dark gaps.',fade:'The whole display fades between colors.',fill:'Fills the line one fixture at a time.',fireworks:'Independent palette-colored bursts bloom and fade across every zone.',lightning:'Brief, irregular flashes of your colors.',march:'Moves the exact color sequence one step at a time.',river:'A wider, flowing version of March.',shuffle:'Randomly rearranges palette colors.',split:'Color travels outward from the center.',sprinkle:'Sparse color sparks fade in and out.',streak:'A single color streak runs along the line.',storm:'Cool white lightning with longer pauses.',takeover:'Each new color gradually replaces the last.',twinkle:'Random palette colors sparkle and fade.'};
const movements=Object.keys(movementInfo),orders=['RGB','RBG','GRB','GBR','BRG','BGR'];
const presets=['#ff3030','#ffffff','#164cff','#19d96f','#ffc21c','#ff6d2d','#9a5cff','#16d8df','#ff3ea5','#101010'];
const themes={midnight:{color:'#080b18'},firework:{color:'#0e0912'},solar:{color:'#100d08'}};
let controller=null,patterns=[],scheduleData={version:1,location:{},weekly:[],holidays:[],runtime:{}},selectedZones=new Set(),currentPattern=null,editor=null,currentView='home',toastTimer,brightnessTimer,githubReleases=[],releasesLoaded=false,releasesLoading=false,wizardStep=0,editingWeekly=-1,editingHoliday=-1;

const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function message(text,error=false){const t=$('toast');t.textContent=text;t.className='toast show'+(error?' error':'');clearTimeout(toastTimer);toastTimer=setTimeout(()=>t.className='toast',2800)}
function setUpdateDrawer(title,detail,progress=null,tone='active'){const drawer=$('updateDrawer'),bar=$('updateDrawerProgress');$('updateDrawerTitle').textContent=title;$('updateDrawerDetail').textContent=detail;drawer.className='update-drawer show'+(tone==='error'?' error':tone==='success'?' success':'');if(progress==null)bar.removeAttribute('value');else bar.value=Math.max(0,Math.min(100,progress));try{localStorage.setItem('leaflights-update-state',JSON.stringify({title,detail,progress,tone,time:Date.now()}))}catch(e){}}
function closeUpdateDrawer(){const drawer=$('updateDrawer');drawer.className='update-drawer';try{localStorage.removeItem('leaflights-update-state')}catch(e){}}
function restoreUpdateDrawer(){try{const state=JSON.parse(localStorage.getItem('leaflights-update-state')||'null');if(state&&Date.now()-state.time<86400000)setUpdateDrawer(state.title,state.detail,state.progress,state.tone)}catch(e){}}
function applyTheme(name,persist=true){if(!themes[name])name='midnight';document.documentElement.dataset.theme=name;document.querySelector('meta[name="theme-color"]').content=themes[name].color;if(persist)try{localStorage.setItem('leaflights-theme',name)}catch(e){}renderThemeChoices()}
function renderThemeChoices(){const active=document.documentElement.dataset.theme||'midnight';document.querySelectorAll('[data-theme-choice]').forEach(button=>{const selected=button.dataset.themeChoice===active;button.classList.toggle('active',selected);button.setAttribute('aria-pressed',selected?'true':'false')})}
async function request(url,options){const r=await fetch(url,options);const text=await r.text();if(!r.ok)throw Error(text||`Request failed (${r.status})`);return text}
function parseColors(p){if(Array.isArray(p.colors))return p.colors;const nums=String(p.colors||'').split(/[,&]/).filter(v=>v!=='').map(Number),out=[];for(let i=0;i+2<nums.length;i+=3)out.push(rgbHex(nums[i],nums[i+1],nums[i+2]));return out.length?out:['#ffffff']}
function rgbHex(r,g,b){return '#'+[r,g,b].map(v=>Math.max(0,Math.min(255,Number(v)||0)).toString(16).padStart(2,'0')).join('')}
function hexRgb(h){h=String(h||'#000000').replace('#','');return [0,2,4].map(i=>parseInt(h.slice(i,i+2),16)||0)}
function normalizePattern(p,i=0){return{id:Number(p.id)||i+1,name:p.name||`Pattern ${i+1}`,type:String(p.type||p.patternType||'stationary').toLowerCase(),direction:String(p.direction||'F').toUpperCase()==='R'?'R':'F',speed:Math.max(1,Math.min(20,Number(p.speed)||10)),gap:Math.max(0,Number(p.gap)||0),pause:Math.max(0,Number(p.pause)||0),other:Math.max(0,Number(p.other)||0),colors:parseColors(p)}}
function serializePattern(p){const values=p.colors.flatMap(hexRgb).join(',')+',';return{id:p.id,name:p.name,type:p.type,num_colors:p.colors.length,direction:p.direction,speed:p.speed,gap:p.gap,pause:p.pause,other:p.other,colors:values}}
function activePatternFromController(){const state=controller?.activePatternState;if(state){const normalized=normalizePattern(state);normalized.id=Number(state.id)||0;normalized.name=state.name||(state.type==='off'?'Lights off':title(state.type));return normalized}return{name:controller?.activePattern==='off'?'Lights off':title(controller?.activePattern||'off'),type:controller?.activePattern||'off',colors:['#000000'],speed:10,direction:'F',gap:0,pause:0,other:0,id:0}}
function selectedList(){if(!selectedZones.size&&controller)controller.zones.forEach((z,i)=>{if(z.enabled)selectedZones.add(i)});return [...selectedZones].sort((a,b)=>a-b)}

async function loadAll(){
  try{const [s,p,c]=await Promise.all([fetch('/api/status'),fetch('/getPatterns'),fetch('/api/schedules')]);if(s.status===401){$('authGate').classList.add('show');return}if(!s.ok||!p.ok||!c.ok)throw Error('Controller did not respond');controller=await s.json();const raw=await p.json();patterns=Array.isArray(raw)?raw.map(normalizePattern):[];scheduleData=await c.json();scheduleData.weekly=Array.isArray(scheduleData.weekly)?scheduleData.weekly:[];scheduleData.holidays=Array.isArray(scheduleData.holidays)?scheduleData.holidays:[];if(!selectedZones.size)controller.zones.forEach((z,i)=>{if(z.enabled)selectedZones.add(i)});currentPattern=activePatternFromController();renderAll();$('connDot').classList.add('online');$('connText').textContent=controller.wifi.connected?'On home Wi‑Fi':'Setup network';if(!controller.setupComplete)startSetupWizard();}
  catch(e){$('connText').textContent='Offline';message(e.message,true)}
}
async function login(e){e.preventDefault();$('loginStatus').textContent='Signing in…';try{await request('/api/login',{method:'POST',body:new URLSearchParams({password:$('loginPassword').value})});$('authGate').classList.remove('show');$('loginPassword').value='';$('loginStatus').textContent='';await loadAll()}catch(error){$('loginStatus').textContent=error.message}}
function startSetupWizard(){if(!controller)return;wizardStep=0;if(!$('wizardCloseButton'))$('wizardForm').insertAdjacentHTML('afterbegin','<button id="wizardCloseButton" class="wizard-close" type="button" onclick="closeSetupWizard()" aria-label="Close setup">×</button>');$('wizardCloseButton').style.display=controller.setupComplete?'block':'none';$('wizardZones').innerHTML=controller.zones.map((z,i)=>`<div id="wizardZone${i}" class="wizard-zone ${z.enabled?'enabled':''}"><div class="wizard-zone-head"><strong>Zone ${i+1}</strong><label class="check"><input type="checkbox" name="wen${i}" ${z.enabled?'checked':''} onchange="toggleWizardZone(${i},this.checked)"> Enabled</label></div><div class="wizard-zone-fields"><label class="field">Name<input name="wname${i}" maxlength="24" value="${esc(z.name||`Zone ${i+1}`)}"></label><label class="field">Fixtures<input name="wcnt${i}" type="number" min="1" max="1000" value="${z.count}"></label><label class="field">Color order<select name="word${i}">${orders.map(o=>`<option ${o===z.order?'selected':''}>${o}</option>`).join('')}</select></label></div></div>`).join('');$('wizardSsid').value=controller.wifi?.ssid||'';$('wizardCompatibility').checked=!!controller.wifi?.compatibilityApEnabled;$('wizardProtectWeb').checked=!!controller.webAccess?.passwordConfigured;renderWizardCompatibility();renderWizardWebPassword();renderWizardStep();$('setupWizard').classList.add('show')}
function toggleWizardZone(index,enabled){$(`wizardZone${index}`).classList.toggle('enabled',enabled)}
function closeSetupWizard(){if(controller?.setupComplete)$('setupWizard').classList.remove('show')}
function renderWizardStep(){document.querySelectorAll('.wizard-panel').forEach((panel,i)=>panel.classList.toggle('active',i===wizardStep));document.querySelectorAll('.wizard-step').forEach((step,i)=>step.classList.toggle('active',i<=wizardStep));$('wizardBackButton').style.visibility=wizardStep?'visible':'hidden';$('wizardNextButton').style.display=wizardStep===3?'none':'block';$('wizardFinishButton').style.display=wizardStep===3?'block':'none'}
function wizardNext(){if(wizardStep===2&&!$('wizardSsid').value&&!$('wizardCompatibility').checked)return message('Configure home Wi-Fi or enable Oelo compatibility',true);if(wizardStep===2&&$('wizardCompatibility').checked&&!controller.setupComplete&&$('wizardCompatibilityPassword').value.length<8)return message('Choose an Oelo network password of at least 8 characters',true);if(wizardStep<3){wizardStep++;renderWizardStep()}}
function wizardPrevious(){if(wizardStep>0){wizardStep--;renderWizardStep()}}
function renderWizardCompatibility(){$('wizardCompatibilityFields').style.display=$('wizardCompatibility').checked?'block':'none'}
function renderWizardWebPassword(){$('wizardWebPasswordFields').style.display=$('wizardProtectWeb').checked?'grid':'none'}
async function finishSetup(e){e.preventDefault();if($('wizardProtectWeb').checked&&$('wizardWebPassword').value!==$('wizardWebPasswordConfirm').value)return message('Web passwords do not match',true);$('wizardStatus').textContent='Saving setup…';try{const text=await request('/api/setup',{method:'POST',body:new URLSearchParams(new FormData($('wizardForm')))});$('wizardStatus').textContent=text;$('connText').textContent='Restarting…'}catch(error){$('wizardStatus').textContent=error.message}}
function renderAll(){renderNav();renderZonePickers();renderHome();renderPatterns();renderSchedules();renderSettings();renderThemeChoices();if(!editor)newPattern(false);renderEditor();renderDiagnostics()}
function showView(name){currentView=name;document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===`view-${name}`));renderNav();scrollTo({top:0,behavior:'smooth'});if(name==='settings'&&!releasesLoaded&&controller?.wifi.connected)setTimeout(()=>loadReleases(false),0);if(name==='schedules')setTimeout(refreshSchedules,0)}
function renderNav(){document.querySelectorAll('[data-nav]').forEach(b=>{b.classList.toggle('active',b.dataset.nav===currentView);b.onclick=()=>{if(b.dataset.nav==='create'&&!editor)newPattern(false);showView(b.dataset.nav)}})}
function renderZonePickers(){if(!controller)return;const html=controller.zones.map((z,i)=>z.enabled?`<button class="chip ${selectedZones.has(i)?'active':''}" onclick="toggleZone(${i})">${esc(z.name)} · ${z.count}</button>`:'').join('');['homeZones','patternZones','editorZones'].forEach(id=>$(id).innerHTML=html)}
function toggleZone(i){selectedZones.has(i)?selectedZones.delete(i):selectedZones.add(i);if(!selectedZones.size)selectedZones.add(i);renderZonePickers()}

function renderHome(){if(!controller)return;const safety=controller.powerSafety||{},maximum=safety.maximumBrightness||204,runtime=scheduleData?.runtime||{},scheduleStatus=runtime.status||'Scheduling not configured';$('homeBrightness').max=maximum;$('homeBrightness').value=Math.min(controller.brightness,maximum);$('brightnessValue').textContent=Math.round(controller.brightness/255*100)+'%';$('powerSafetyLabel').textContent=`${safety.budgetPercent||80}% safety ceiling · ${safety.supplyVolts||36} V / ${((safety.supplyMilliamps||5600)/1000).toFixed(1)} A supply`;$('nowName').textContent=currentPattern?.name||'Lights off';$('nowMode').textContent=title(currentPattern?.type||'off');$('homeScheduleStatus').textContent=scheduleStatus==='No enabled schedule rules'?'No schedule configured':scheduleStatus;$('homeScheduleNext').textContent=runtime.nextEvent?`Next: ${friendlyDateTime(runtime.nextEvent,runtime.now)}`:runtime.clockValid===false?'Waiting for network time':'No upcoming scheduled change';buildPreview($('livePreview'),40);$('quickColors').innerHTML=presets.slice(0,8).map(c=>`<button class="swatch" style="--c:${c}" aria-label="Set ${c}" onclick="applySolid('${c}')"></button>`).join('')+`<label class="swatch custom-color" aria-label="Choose a color"><input type="color" value="#6f8cff" onchange="applySolid(this.value)"></label>`;$('quickPatterns').innerHTML=patterns.length?patterns.slice(0,2).map((p,i)=>patternCard(p,i,false)).join(''):'<div class="card empty">No saved patterns yet.</div>';hydratePreviews()}
function renderPatterns(){const grid=$('patternGrid');if(!grid)return;grid.innerHTML=patterns.length?patterns.map((p,i)=>patternCard(p,i,true)).join(''):'<div class="card empty">No saved patterns. Create the first one.</div>';hydratePreviews()}
function patternCard(p,i,editable){return`<article class="card pattern-card"><div class="pattern-title"><div><h3>${esc(p.name)}</h3><span>${esc(title(p.type))}</span></div><span>${p.colors.length} colors</span></div><div class="preview" data-pattern-index="${i}"></div><div class="pattern-meta"><span class="tag">Speed ${p.speed}</span><span class="tag">${p.direction==='R'?'Reverse':'Forward'}</span>${p.gap?`<span class="tag">Gap ${p.gap}</span>`:''}</div><div class="actions"><button class="btn primary" onclick="playPattern(${i})">▶ Play</button>${editable?`<button class="btn" onclick="editPattern(${i})">Edit</button><button class="btn ghost" onclick="deletePattern(${i})" aria-label="Delete ${esc(p.name)}">Delete</button>`:''}</div></article>`}

async function playPattern(i){await applyPattern(patterns[i])}
async function applyPattern(p){try{const q=new URLSearchParams({zones:selectedList().join(','),patternType:p.type,patternId:p.id||0,patternName:p.name||title(p.type),num_colors:p.colors.length,colors:p.colors.flatMap(hexRgb).join(',')+',',speed:p.speed,gap:p.gap,pause:p.pause,other:p.other,direction:p.direction});await request('/setPattern?'+q);currentPattern={...p};if(controller){controller.activePattern=p.type;controller.activePatternState={...p}}renderHome();message(`${p.name} is playing`)}catch(e){message(e.message,true)}}
async function applySolid(color){await applyPattern({name:'Custom color',type:'stationary',colors:[color],speed:10,gap:0,pause:0,other:0,direction:'F'})}
async function allOff(){try{await request('/api/off');currentPattern={name:'Lights off',type:'off',colors:['#000000'],speed:10,direction:'F',gap:0,other:0};renderHome();message('All lights are off')}catch(e){message(e.message,true)}}
function brightnessPreview(v){$('brightnessValue').textContent=Math.round(v/255*100)+'%'}
function setBrightness(v){clearTimeout(brightnessTimer);brightnessTimer=setTimeout(async()=>{try{await request('/api/brightness?value='+v);controller.brightness=Number(v);message('Brightness updated')}catch(e){message(e.message,true)}},100)}

function newPattern(navigate=true){editor={id:null,name:'',type:'stationary',direction:'F',speed:10,gap:0,pause:0,other:0,colors:['#ff3030','#ffffff','#164cff']};renderEditor();if(navigate)showView('create')}
function editPattern(i){editor={...patterns[i],colors:[...patterns[i].colors]};renderEditor();showView('create')}
function renderEditor(){if(!editor)return;$('editorHeading').textContent=editor.id?'Edit pattern':'New pattern';$('patternName').value=editor.name;$('patternSpeed').value=editor.speed;$('speedOut').textContent=editor.speed;$('patternDirection').value=editor.direction;$('patternGap').value=editor.gap;$('patternPause').value=editor.pause;$('patternOther').value=editor.other;$('movementDescription').textContent=movementInfo[editor.type]||'';$('movementGrid').innerHTML=movements.map(m=>`<button class="movement ${m===editor.type?'active':''}" onclick="chooseMovement('${m}')"><span class="mini" data-mini="${m}"></span>${title(m)}</button>`).join('');renderPalette();updateEditorPreview();hydratePreviews()}
function readEditor(){if(!editor)return;editor.name=$('patternName').value;editor.speed=Number($('patternSpeed').value);editor.direction=$('patternDirection').value;editor.gap=Math.max(0,Number($('patternGap').value)||0);editor.pause=Math.max(0,Number($('patternPause').value)||0);editor.other=Math.max(0,Number($('patternOther').value)||0);updateEditorPreview()}
function chooseMovement(m){readEditor();editor.type=m;renderEditor()}
function renderPalette(){$('colorCount').textContent=`${editor.colors.length} color${editor.colors.length===1?'':'s'}`;$('paletteEditor').innerHTML=editor.colors.map((c,i)=>`<label class="color-edit" style="--c:${c}"><input type="color" value="${c}" onchange="setEditorColor(${i},this.value)">${editor.colors.length>1?`<button type="button" onclick="event.preventDefault();removeEditorColor(${i})">×</button>`:''}</label>`).join('')+`<button class="color-edit add-color" onclick="addEditorColor()" title="Add color">＋</button>`;$('editorPresets').innerHTML=presets.map(c=>`<button class="swatch" style="--c:${c}" onclick="addEditorColor('${c}')" aria-label="Add ${c}"></button>`).join('')}
function setEditorColor(i,c){editor.colors[i]=c;renderPalette();updateEditorPreview()}
function addEditorColor(c='#6f8cff'){if(editor.colors.length>=128)return message('The controller supports up to 128 colors',true);editor.colors.push(c);renderPalette();updateEditorPreview()}
function removeEditorColor(i){if(editor.colors.length<=1)return;editor.colors.splice(i,1);renderPalette();updateEditorPreview()}
function updateEditorPreview(){$('previewName').textContent=editor.name||'Untitled pattern';$('editorSummary').innerHTML=`<strong>${title(editor.type)}</strong><br>${esc(movementInfo[editor.type])}<br>Speed ${editor.speed} · ${editor.direction==='R'?'Reverse':'Forward'} · ${editor.colors.length} color${editor.colors.length===1?'':'s'}`;buildPreview($('editorPreview'),40)}
async function testEditor(){readEditor();await applyPattern({...editor,name:editor.name||'Unsaved pattern'})}
async function saveEditor(){readEditor();if(!editor.name.trim())return message('Give the pattern a name first',true);if(editor.id==null){editor.id=Math.max(0,...patterns.map(p=>Number(p.id)||0))+1;patterns.push({...editor,colors:[...editor.colors]})}else{const i=patterns.findIndex(p=>p.id===editor.id);if(i>=0)patterns[i]={...editor,colors:[...editor.colors]};else patterns.push({...editor,colors:[...editor.colors]})}try{await persistPatterns();message('Pattern saved');renderPatterns();renderHome();showView('patterns')}catch(e){message(e.message,true)}}
async function deletePattern(i){if(!confirm(`Delete “${patterns[i].name}”?`))return;const removed=patterns.splice(i,1)[0];try{await persistPatterns();renderPatterns();renderHome();message(`${removed.name} deleted`)}catch(e){patterns.splice(i,0,removed);message(e.message,true)}}
async function persistPatterns(){await request('/api/patterns',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(patterns.map(serializePattern))})}

const dayNames=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'],monthNames=['January','February','March','April','May','June','July','August','September','October','November','December'];
function formatClock(minutes){minutes=Math.max(0,Math.min(1439,Number(minutes)||0));const hour=Math.floor(minutes/60),minute=minutes%60;return`${String(hour).padStart(2,'0')}:${String(minute).padStart(2,'0')}`}
function friendlyDateTime(value,reference='',includeDate=false){const match=String(value||'').match(/^(\d{4})-(\d{2})-(\d{2})\s+(.+)$/);if(!match)return value||'';const date=match[1]+'-'+match[2]+'-'+match[3],time=match[4],referenceDate=String(reference||'').slice(0,10),parsed=new Date(`${date}T12:00:00`);if(!includeDate&&date===referenceDate)return`Today · ${time}`;const label=parsed.toLocaleDateString(undefined,{weekday:'short',month:'short',day:'numeric'});return`${label} · ${time}`}
function describeTime(value={}){if((value.type||'clock')==='clock'){const m=Number(value.minutes)||0,h=Math.floor(m/60),mins=m%60;return`${((h+11)%12)+1}:${String(mins).padStart(2,'0')} ${h>=12?'PM':'AM'}`}const offset=Number(value.offset)||0;return`${Math.abs(offset)} min ${offset<0?'before':offset>0?'after':'at'} ${value.type}`.replace('0 min at ', '')}
function describeDays(mask){const days=dayNames.filter((_,i)=>Number(mask)&(1<<i));return days.length===7?'Every day':days.join(', ')||'No days'}
function describeZones(mask){if(!controller)return'All zones';const names=controller.zones.filter((z,i)=>z.enabled&&(!Number(mask)||(Number(mask)&(1<<i)))).map(z=>z.name);return names.length===controller.zones.filter(z=>z.enabled).length?'All zones':names.join(', ')||'No zones'}
function patternName(id){return patterns.find(p=>p.id===Number(id))?.name||(Number(id)?`Missing pattern ${id}`:'Lights off')}
function schedulePatternOptions(selected){return`<option value="0">Lights off</option>`+patterns.map(p=>`<option value="${p.id}" ${Number(selected)===p.id?'selected':''}>${esc(p.name)}</option>`).join('')}
function renderSchedules(){if(!controller||!scheduleData)return;const runtime=scheduleData.runtime||{},location=scheduleData.location||{};$('scheduleStatus').textContent=runtime.status||'No enabled schedule rules';$('scheduleNow').textContent=runtime.now?friendlyDateTime(runtime.now,runtime.now,true):'Waiting for network time';$('scheduleNext').textContent=runtime.nextEvent?friendlyDateTime(runtime.nextEvent,runtime.now):'No upcoming change';$('manualOverrideStatus').textContent=runtime.manualOverride?`${runtime.manualDescription||'Manual override'}${runtime.manualUntil?' · until '+friendlyDateTime(runtime.manualUntil,runtime.now):' · until resumed'}`:'Schedule control is active';$('scheduleTimezone').value=location.timezone||'CST6CDT,M3.2.0/2,M11.1.0/2';$('scheduleLatitude').value=location.latitude??41.8781;$('scheduleLongitude').value=location.longitude??-87.6298;renderWeeklySchedules();renderHolidaySchedules()}
function renderWeeklySchedules(){const list=$('weeklyScheduleList');list.innerHTML=scheduleData.weekly.length?scheduleData.weekly.map((r,i)=>`<article class="card schedule-card"><div><div class="pattern-title" style="margin:0"><h3>${esc(r.name||'Weekly rule')}</h3><span>${r.enabled===false?'Paused':'Active'}</span></div><p>${describeDays(r.days)} · ${describeTime(r.on)}–${describeTime(r.off)}<br>${esc(patternName(r.patternId))} · ${esc(describeZones(r.zones))}</p></div><div class="actions"><button class="btn" onclick="editWeeklyRule(${i})">Edit</button><button class="btn ghost" onclick="deleteScheduleRule('weekly',${i})">Delete</button></div></article>`).join(''):'<div class="card empty">No weekly routine yet. Add one to automate normal nights.</div>'}
function renderHolidaySchedules(){const list=$('holidayScheduleList');list.innerHTML=scheduleData.holidays.length?scheduleData.holidays.map((r,i)=>`<article class="card schedule-card"><div><div class="pattern-title" style="margin:0"><h3>${esc(r.name||'Holiday')}</h3><span>${r.enabled===false?'Paused':'Override'}</span></div><p>${monthNames[(Number(r.month)||1)-1]} ${Number(r.day)||1} · ${Number(r.daysBefore)||0} day(s) before / ${Number(r.daysAfter)||0} after<br>${describeTime(r.on)}–${describeTime(r.off)} · ${esc(patternName(r.patternId))} · ${esc(describeZones(r.zones))}</p></div><div class="actions"><button class="btn" onclick="editHolidayRule(${i})">Edit</button><button class="btn ghost" onclick="deleteScheduleRule('holidays',${i})">Delete</button></div></article>`).join(''):'<div class="card empty">No holiday overrides yet.</div>'}
function renderTimeExpression(container,prefix,value={type:'clock',minutes:1080,offset:0}){$(container).innerHTML=`<label class="field">Timing<select id="${prefix}Type" onchange="updateTimeExpression('${prefix}')"><option value="clock">At a time</option><option value="sunset">Relative to sunset</option><option value="sunrise">Relative to sunrise</option></select></label><label class="field" id="${prefix}ClockWrap">Time<input id="${prefix}Clock" type="time" value="${formatClock(value.minutes??1080)}"></label><label class="field" id="${prefix}OffsetWrap">Offset in minutes<input id="${prefix}Offset" type="number" min="-720" max="720" value="${Number(value.offset)||0}"><small>Negative is before; positive is after.</small></label>`;$(`${prefix}Type`).value=value.type||'clock';updateTimeExpression(prefix)}
function updateTimeExpression(prefix){const solar=$(`${prefix}Type`).value!=='clock';$(`${prefix}ClockWrap`).style.display=solar?'none':'block';$(`${prefix}OffsetWrap`).style.display=solar?'block':'none'}
function readTimeExpression(prefix){const type=$(`${prefix}Type`).value;if(type==='clock'){const [hour,minute]=($(`${prefix}Clock`).value||'00:00').split(':').map(Number);return{type,minutes:hour*60+minute}}return{type,offset:Math.max(-720,Math.min(720,Number($(`${prefix}Offset`).value)||0))}}
function renderRuleZones(container,prefix,mask=0){$(container).innerHTML=controller.zones.map((z,i)=>z.enabled?`<label class="toggle-chip"><input id="${prefix}Zone${i}" type="checkbox" ${!Number(mask)||(Number(mask)&(1<<i))?'checked':''}><span>${esc(z.name)}</span></label>`:'').join('')}
function readRuleZones(prefix){let mask=0;controller.zones.forEach((z,i)=>{if(z.enabled&&$(`${prefix}Zone${i}`)?.checked)mask|=1<<i});return mask}
function editWeeklyRule(index){editingWeekly=index;const rule=index>=0?scheduleData.weekly[index]:{name:'Weeknight lights',enabled:true,days:62,on:{type:'sunset',offset:-20},off:{type:'clock',minutes:1380},patternId:patterns[0]?.id||0,zones:0,priority:0};$('weeklyEditorTitle').textContent=index>=0?'Edit weekly rule':'New weekly rule';$('weeklyName').value=rule.name||'';$('weeklyEnabled').checked=rule.enabled!==false;$('weeklyDays').innerHTML=dayNames.map((day,i)=>`<label class="toggle-chip"><input id="weeklyDay${i}" type="checkbox" ${Number(rule.days)&(1<<i)?'checked':''}><span>${day}</span></label>`).join('');renderTimeExpression('weeklyOnExpression','weeklyOn',rule.on);renderTimeExpression('weeklyOffExpression','weeklyOff',rule.off);$('weeklyPattern').innerHTML=schedulePatternOptions(rule.patternId);renderRuleZones('weeklyZones','weekly',rule.zones);$('weeklyRuleEditor').classList.add('show');$('weeklyRuleEditor').scrollIntoView({behavior:'smooth',block:'start'})}
function editHolidayRule(index){editingHoliday=index;const rule=index>=0?scheduleData.holidays[index]:{name:'Fourth of July',enabled:true,month:7,day:4,daysBefore:2,daysAfter:1,on:{type:'sunset',offset:-20},off:{type:'clock',minutes:1380},patternId:patterns[0]?.id||0,zones:0,priority:100};$('holidayEditorTitle').textContent=index>=0?'Edit holiday override':'New holiday override';$('holidayName').value=rule.name||'';$('holidayEnabled').checked=rule.enabled!==false;$('holidayMonth').innerHTML=monthNames.map((name,i)=>`<option value="${i+1}">${name}</option>`).join('');$('holidayMonth').value=Number(rule.month)||1;$('holidayDay').value=Number(rule.day)||1;$('holidayBefore').value=Number(rule.daysBefore)||0;$('holidayAfter').value=Number(rule.daysAfter)||0;renderTimeExpression('holidayOnExpression','holidayOn',rule.on);renderTimeExpression('holidayOffExpression','holidayOff',rule.off);$('holidayPattern').innerHTML=schedulePatternOptions(rule.patternId);renderRuleZones('holidayZones','holiday',rule.zones);$('holidayRuleEditor').classList.add('show');$('holidayRuleEditor').scrollIntoView({behavior:'smooth',block:'start'})}
function closeRuleEditor(type){$(`${type}RuleEditor`).classList.remove('show');if(type==='weekly')editingWeekly=-1;else editingHoliday=-1}
function nextScheduleId(){return Math.max(0,...scheduleData.weekly.concat(scheduleData.holidays).map(r=>Number(r.id)||0))+1}
async function saveWeeklyRule(event){event.preventDefault();let days=0;dayNames.forEach((_,i)=>{if($(`weeklyDay${i}`).checked)days|=1<<i});if(!days)return message('Choose at least one day',true);const previous=editingWeekly>=0?scheduleData.weekly[editingWeekly]:null,rule={id:previous?.id||nextScheduleId(),name:$('weeklyName').value.trim(),enabled:$('weeklyEnabled').checked,days,on:readTimeExpression('weeklyOn'),off:readTimeExpression('weeklyOff'),patternId:Number($('weeklyPattern').value)||0,zones:readRuleZones('weekly'),priority:Number(previous?.priority)||0};if(editingWeekly>=0)scheduleData.weekly[editingWeekly]=rule;else scheduleData.weekly.push(rule);if(await saveSchedules('Weekly schedule saved'))closeRuleEditor('weekly')}
async function saveHolidayRule(event){event.preventDefault();const previous=editingHoliday>=0?scheduleData.holidays[editingHoliday]:null,rule={id:previous?.id||nextScheduleId(),name:$('holidayName').value.trim(),enabled:$('holidayEnabled').checked,month:Number($('holidayMonth').value),day:Number($('holidayDay').value),daysBefore:Number($('holidayBefore').value)||0,daysAfter:Number($('holidayAfter').value)||0,on:readTimeExpression('holidayOn'),off:readTimeExpression('holidayOff'),patternId:Number($('holidayPattern').value)||0,zones:readRuleZones('holiday'),priority:Number(previous?.priority)||100};if(editingHoliday>=0)scheduleData.holidays[editingHoliday]=rule;else scheduleData.holidays.push(rule);if(await saveSchedules('Holiday override saved'))closeRuleEditor('holiday')}
async function deleteScheduleRule(type,index){const rule=scheduleData[type][index];if(!confirm(`Delete “${rule.name}”?`))return;scheduleData[type].splice(index,1);await saveSchedules('Schedule deleted')}
function schedulePayload(){return{version:1,location:scheduleData.location||{},weekly:scheduleData.weekly||[],holidays:scheduleData.holidays||[]}}
async function saveSchedules(success){try{await request('/api/schedules',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(schedulePayload())});message(success);await refreshSchedules();return true}catch(error){message(error.message,true);return false}}
async function saveScheduleLocation(event){event.preventDefault();scheduleData.location={timezone:$('scheduleTimezone').value,latitude:Number($('scheduleLatitude').value),longitude:Number($('scheduleLongitude').value)};await saveSchedules('Location and time zone saved')}
function useBrowserLocation(){if(!navigator.geolocation)return message('Location is not available in this browser',true);navigator.geolocation.getCurrentPosition(position=>{$('scheduleLatitude').value=position.coords.latitude.toFixed(4);$('scheduleLongitude').value=position.coords.longitude.toFixed(4);message('Location filled in; press Save location')},error=>message(error.message,true),{enableHighAccuracy:false,timeout:10000})}
async function refreshSchedules(){try{const response=await fetch('/api/schedules');if(!response.ok)throw Error(await response.text());scheduleData=await response.json();scheduleData.weekly=Array.isArray(scheduleData.weekly)?scheduleData.weekly:[];scheduleData.holidays=Array.isArray(scheduleData.holidays)?scheduleData.holidays:[];renderSchedules()}catch(error){message(error.message,true)}}
async function refreshHomeStatus(){try{const [statusResponse,scheduleResponse]=await Promise.all([fetch('/api/status',{cache:'no-store'}),fetch('/api/schedules',{cache:'no-store'})]);if(!statusResponse.ok||!scheduleResponse.ok)return;controller=await statusResponse.json();scheduleData=await scheduleResponse.json();scheduleData.weekly=Array.isArray(scheduleData.weekly)?scheduleData.weekly:[];scheduleData.holidays=Array.isArray(scheduleData.holidays)?scheduleData.holidays:[];currentPattern=activePatternFromController();renderHome()}catch(error){}}
async function setManualOverride(action){try{const body=new URLSearchParams({action,duration:'next'});message(await request('/api/manual-override',{method:'POST',body}));setTimeout(refreshSchedules,350)}catch(error){message(error.message,true)}}

function renderSettings(){if(!controller)return;const w=controller.wledSync||{},ota=controller.ota||{},wifi=controller.wifi||{},access=controller.webAccess||{};$('wledEnabled').checked=!!w.enabled;$('wledDestination').value=w.destination||'255.255.255.255';$('wledPixels').value=w.pixelCount||300;$('wledSource').innerHTML='<option value="-1">Auto (first active)</option>'+controller.zones.map((z,i)=>`<option value="${i}">${i+1}: ${esc(z.name)}</option>`).join('');$('wledSource').value=String(w.sourceZone??-1);$('wledStatus').textContent=`Status: ${w.status||'Disabled'}`;$('firmwareVersion').textContent=`Current firmware: ${controller.firmwareVersion||'development build'} · maximum image ${formatBytes(ota.maxImageBytes||0)}`;$('automaticUpdates').checked=!!ota.automaticUpdates;$('automaticUpdateStatus').textContent=ota.automaticUpdateStatus||'Automatic updates disabled';$('firmwareUpdateStatus').textContent='Firmware changes use the current web session.';$('ssid').value=wifi.ssid||'';$('compatibilityApEnabled').checked=!!wifi.compatibilityApEnabled;$('compatibilityStatus').innerHTML=wifi.compatibilityApFallback?'<strong>Protected recovery network active.</strong> Home Wi-Fi failed to connect, so LeafLights-Setup was temporarily restored with WPA2.':wifi.compatibilityApActive?'<strong>WPA2 compatibility network active.</strong> Join OELO_1-23.0 in phone settings before opening LeafFilter local control.':'<strong>Compatibility network disabled.</strong> The controller is available only through home Wi-Fi.';$('webAccessStatus').innerHTML=access.passwordConfigured?'<strong>Web login enabled.</strong> This browser session covers controls, settings, restarts, and firmware updates.':'<strong>Web login disabled.</strong> Anyone already connected to an authorized Wi-Fi network can open the interface.';$('removeWebPasswordButton').style.display=access.passwordConfigured?'inline-block':'none';$('zonesForm').innerHTML=controller.zones.map((z,i)=>`<div class="zone-config"><label class="check"><input type="checkbox" name="en${i}" ${z.enabled?'checked':''}> On</label><label class="field">Name<input name="name${i}" maxlength="24" value="${esc(z.name)}"></label><label class="field count">Fixtures<input name="cnt${i}" type="number" min="1" max="1000" value="${z.count}"></label><label class="field order">Order<select name="ord${i}">${orders.map(o=>`<option ${o===z.order?'selected':''}>${o}</option>`).join('')}</select></label></div>`).join('')}
async function saveWled(e){e.preventDefault();try{const data=new URLSearchParams(new FormData($('wledForm')));if(!$('wledEnabled').checked)data.set('enabled','0');const text=await request('/api/wled-sync',{method:'POST',body:data});message(text);await loadAll()}catch(err){message(err.message,true)}}
function formatBytes(value){if(!value)return 'unknown';return(value/1024/1024).toFixed(2)+' MB'}
function renderReleases(){const list=$('releaseList');if(!githubReleases.length){list.innerHTML='<div class="empty">No compatible GitHub releases found.</div>';return}list.innerHTML=githubReleases.map((release,i)=>`<article class="release-card"><header><div><h3>${esc(release.name||release.tag)}</h3><div class="firmware-state">${esc(release.tag)} · ${formatBytes(release.size)}${release.prerelease?' · prerelease':''}</div></div>${release.newer?'<span class="tag">Update</span>':''}</header><div class="notes">${esc(release.notes||'No release notes were provided.')}</div><button class="btn ${release.newer?'primary':''}" onclick="installRelease(${i})">${release.newer?'Install update':'Install this release'}</button></article>`).join('')}
async function loadReleases(force=false){if(releasesLoading||(!force&&releasesLoaded))return;if(!controller?.wifi.connected){$('releaseList').innerHTML='<div class="empty">Connect the controller to home Wi-Fi before checking GitHub.</div>';return}releasesLoading=true;$('releaseList').innerHTML='<div class="empty">Checking GitHub releases…</div>';try{const response=await fetch('/api/releases');const text=await response.text();if(!response.ok)throw Error(text||`Release check failed (${response.status})`);const data=JSON.parse(text);githubReleases=data.releases||[];releasesLoaded=true;renderReleases()}catch(error){$('releaseList').innerHTML=`<div class="empty">${esc(error.message)}</div>`;message(error.message,true)}finally{releasesLoading=false}}
const sleep=milliseconds=>new Promise(resolve=>setTimeout(resolve,milliseconds));
const cleanVersion=value=>String(value||'').trim().replace(/^v/i,'').replace(/-dev$/i,'');
const connectionFailure=error=>error instanceof TypeError||/failed to fetch|network|connection|load failed/i.test(error?.message||'');
async function waitForFirmwareRestart(expectedVersion=''){const status=$('firmwareUpdateStatus'),expected=cleanVersion(expectedVersion),deadline=Date.now()+90000;status.textContent='Update transferred. Waiting for the controller to restart…';setUpdateDrawer('Restarting controller','The update was transferred. Reconnecting and verifying the running firmware…',100);await sleep(7000);while(Date.now()<deadline){try{const response=await fetch('/api/status',{cache:'no-store'});if(response.ok){const running=await response.json();if(!expected||cleanVersion(running.firmwareVersion)===expected){controller=running;status.textContent=expected?`Firmware ${running.firmwareVersion} is running.`:'Controller restarted after the firmware update.';setUpdateDrawer('Update complete',status.textContent,100,'success');setTimeout(loadAll,250);return true}}}catch(error){}await sleep(2000)}throw Error(expected?`The controller did not return running ${expectedVersion} within 90 seconds.`:'The controller did not reconnect within 90 seconds.')}
async function installRelease(index){const release=githubReleases[index];if(!confirm(`Install ${release.tag}? The controller will reboot after verification.`))return;const status=$('firmwareUpdateStatus');status.textContent=`Downloading and verifying ${release.tag}…`;setUpdateDrawer(`Installing ${release.tag}`,'Downloading the signed release from GitHub and verifying its SHA-256 digest.',null);try{const body=new URLSearchParams({tag:release.tag,url:release.url,digest:release.digest,size:String(release.size)});try{await request('/api/install-release',{method:'POST',body})}catch(error){if(!connectionFailure(error))throw error}await waitForFirmwareRestart(release.tag)}catch(error){status.textContent=error.message;setUpdateDrawer('Update failed',error.message,0,'error')}}
async function saveAutomaticUpdates(e){e.preventDefault();const enabled=$('automaticUpdates').checked;try{const text=await request('/api/automatic-updates',{method:'POST',body:new URLSearchParams({enabled:enabled?'1':'0'})});controller.ota.automaticUpdates=enabled;controller.ota.automaticUpdateStatus=text;$('automaticUpdateStatus').textContent=text;message(text)}catch(error){message(error.message,true)}}
function uploadFirmware(e){e.preventDefault();const file=$('firmwareFile').files[0];if(!file)return message('Choose a firmware .bin file',true);if(!file.name.toLowerCase().endsWith('.bin'))return message('The firmware image must end in .bin',true);if(!confirm(`Install ${file.name} and reboot the controller?`))return;const form=new FormData();form.append('firmware',file,file.name);const xhr=new XMLHttpRequest(),status=$('firmwareUpdateStatus');let uploadedPercent=0,reconnectStarted=false;const reconnect=async()=>{if(reconnectStarted)return;reconnectStarted=true;try{await waitForFirmwareRestart()}catch(error){status.textContent=error.message;setUpdateDrawer('Update status unknown',error.message,100,'error')}};status.textContent='Uploading firmware…';setUpdateDrawer('Uploading firmware',file.name,0);xhr.open('POST','/api/update');xhr.upload.onprogress=event=>{if(event.lengthComputable){uploadedPercent=Math.round(event.loaded/event.total*100);status.textContent=`Uploading firmware… ${uploadedPercent}%`;setUpdateDrawer('Uploading firmware',`${file.name} · ${uploadedPercent}%`,uploadedPercent)}};xhr.onerror=()=>{if(uploadedPercent>=95)reconnect();else{status.textContent='Upload connection failed.';setUpdateDrawer('Update failed','The upload connection was interrupted before the firmware was transferred.',uploadedPercent,'error')}};xhr.onload=()=>{if(xhr.status===200||(!xhr.status&&uploadedPercent>=95))reconnect();else{status.textContent=xhr.responseText||`Update failed (${xhr.status})`;setUpdateDrawer('Update failed',status.textContent,uploadedPercent,'error')}};xhr.send(form)}
async function saveZones(e){e.preventDefault();try{message(await request('/api/zones',{method:'POST',body:new URLSearchParams(new FormData($('zonesForm')))}));$('connText').textContent='Rebooting…'}catch(err){message(err.message,true)}}
async function saveNetwork(e){e.preventDefault();try{message(await request('/api/network',{method:'POST',body:new URLSearchParams(new FormData($('networkForm')))}));$('connText').textContent='Rebooting…'}catch(err){message(err.message,true)}}
async function saveWebPassword(e){e.preventDefault();const password=$('webPassword').value,confirmPassword=$('webPasswordConfirm').value;if(password.length<8)return message('Use at least 8 characters',true);if(password!==confirmPassword)return message('Passwords do not match',true);try{message(await request('/api/web-password',{method:'POST',body:new URLSearchParams({enabled:'1',password})}));$('webPassword').value='';$('webPasswordConfirm').value='';await loadAll()}catch(err){message(err.message,true)}}
async function removeWebPassword(e){e.preventDefault();if(!confirm('Remove the web interface login?'))return;try{message(await request('/api/web-password',{method:'POST',body:new URLSearchParams({enabled:'0'})}));await loadAll()}catch(err){message(err.message,true)}}
async function restartDevice(e){e.preventDefault();if(!confirm('Restart the controller now? The lights will briefly turn off.'))return;try{const text=await request('/api/restart',{method:'POST',body:''});$('connText').textContent='Restarting…';message(text)}catch(err){message(err.message,true)}}
function renderDiagnostics(){if(!controller)return;$('statusBox').textContent=JSON.stringify(controller,null,2)}

function title(s){return String(s||'').replace(/(^|[-_ ])(\w)/g,(_,a,b)=>(a?' ':'')+b.toUpperCase())}
function buildPreview(el,count=32){if(!el)return;if(el.children.length!==count)el.innerHTML='<i class="led"></i>'.repeat(count)}
function hydratePreviews(){document.querySelectorAll('[data-pattern-index]').forEach(el=>buildPreview(el,Math.min(40,Math.max(24,patterns[Number(el.dataset.patternIndex)]?.colors.length||24))));document.querySelectorAll('[data-mini]').forEach(el=>{if(!el.children.length)el.innerHTML='<i></i>'.repeat(10)});buildPreview($('livePreview'),40);buildPreview($('editorPreview'),40)}
function mix(a,b,t){const x=hexRgb(a),y=hexRgb(b);return rgbHex(...x.map((v,i)=>Math.round(v+(y[i]-v)*t)))}
function mod(n,m){return((n%m)+m)%m}
function hash(i,t){let x=((i+1)*2654435761+(t+7)*1013904223)>>>0;x^=x>>>16;return x>>>0}
function frame(p,n,t){const c=p?.colors?.length?p.colors:['#000000'],type=p?.type||'off',rev=p?.direction==='R'?-1:1,step=t*rev,out=Array(n).fill('#000000');if(type==='off')return out;
  if(type==='stationary')return out.map((_,i)=>c[i%c.length]);
  if(type==='march'||type==='river'){const group=type==='river'?Math.max(2,p.other||3):1;return out.map((_,i)=>c[mod(Math.floor(i/group)+step,c.length)])}
  if(type==='blend'){return out.map((_,i)=>{const x=mod((i/n*c.length)+(t*.08*rev),c.length),a=Math.floor(x),b=(a+1)%c.length;return mix(c[a],c[b],x-a)})}
  if(type==='fade'){const x=(t*.08)%c.length,a=Math.floor(x),b=(a+1)%c.length;return out.fill(mix(c[a],c[b],x-a))}
  if(type==='chase'){const lit=Math.max(1,p.other||2),gap=Math.max(2,p.gap||4),period=lit+gap;return out.map((_,i)=>mod(i+step,period)<lit?c[mod(Math.floor((i+step)/period),c.length)]:'#000000')}
  if(type==='split')return out.map((_,i)=>c[mod(Math.floor(Math.abs(i-(n-1)/2))+step,c.length)]);
  if(type==='fill'||type==='takeover'){const head=((step%n)+n)%n,col=c[Math.floor(t/n)%c.length];return out.map((_,i)=>(type==='takeover'||i<=head)?col:'#000000')}
  if(type==='shuffle')return out.map((_,i)=>c[hash(i,Math.floor(t/3))%c.length]);
  if(type==='arcade'){const h=((step%n)+n)%n;out[h]=c[Math.floor(t/n)%c.length];out[(h-rev+n)%n]=mix(c[Math.floor(t/n)%c.length],'#000000',.65);return out}
  if(type==='streak'||type==='bolt'){const h=((step%n)+n)%n,len=type==='bolt'?Math.min(8,c.length+2):4;for(let j=0;j<len;j++)out[(h-j*rev+n)%n]=mix(c[j%c.length],'#000000',j/len*.82);return out}
  if(type==='fireworks'){for(let b=0;b<3;b++){const shifted=t+b*9,phase=mod(shifted,27);if(phase>16)continue;const cycle=Math.floor(shifted/27),center=hash(b,cycle)%n,col=c[hash(cycle,b)%c.length],spark=mix(col,'#000000',phase/21);[center-phase,center+phase,center-Math.floor(phase/2),center+Math.floor(phase/2)].forEach(i=>{if(i>=0&&i<n)out[i]=phase<2?'#ffffff':spark})}return out}
  if(type==='lightning'||type==='storm'){const flash=(t%(type==='storm'?21:13))<2;return out.fill(flash?(type==='storm'?'#d9e8ff':c[t%c.length]):'#000000')}
  if(type==='twinkle'||type==='sprinkle'){return out.map((_,i)=>{const h=hash(i,Math.floor(t/2)),chance=type==='twinkle'?5:8;return h%chance===0?c[h%c.length]:mix(c[h%c.length],'#000000',.9)})}
  return out.map((_,i)=>c[mod(i+step,c.length)]);
}
function paint(el,p,t,mini=false){const colors=frame(p,el.children.length,t);[...el.children].forEach((led,i)=>{const color=colors[i]||'#000';led.style.background=color;if(!mini){led.style.setProperty('--c',color);led.classList.toggle('on',color!=='#000000'&&color!=='#000')}})}
let tick=0;setInterval(()=>{tick++;document.querySelectorAll('[data-pattern-index]').forEach(el=>paint(el,patterns[Number(el.dataset.patternIndex)],tick));document.querySelectorAll('[data-mini]').forEach(el=>paint(el,{type:el.dataset.mini,colors:['#ff3030','#ffffff','#164cff'],speed:10,direction:'F',gap:3,other:2},tick,true));if(currentPattern)paint($('livePreview'),currentPattern,tick);if(editor)paint($('editorPreview'),editor,tick)},120);
setInterval(()=>{if(currentView==='schedules')refreshSchedules()},30000);
setInterval(()=>{if(currentView==='home')refreshHomeStatus()},15000);
document.addEventListener('DOMContentLoaded',()=>{applyTheme(document.documentElement.dataset.theme||'midnight',false);restoreUpdateDrawer();if(matchMedia('(max-width:800px)').matches)document.querySelectorAll('#view-settings details[open],#view-schedules details[open]').forEach(item=>item.removeAttribute('open'));showView('home');hydratePreviews();loadAll()});
</script>
</body>
</html>
)HTML";
